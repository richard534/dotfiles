#!/usr/bin/env bash

# ls
alias l='ls -lAGh' # long-fmt, all, colours, human-readable-sizes

# emacs
alias em='emacsclient --no-wait'

# tree
# Use colours by default
alias tree='tree -C'

# misc
alias lt='du -sh * | sort -h'
alias gh='history|grep'
